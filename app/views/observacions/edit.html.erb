<div class="max-w-4xl mx-auto bg-gray-900 p-6 rounded-lg shadow-md">
  <h2 class="text-xl font-semibold text-white mb-4">
    <%= action_name == "new" ? "Nueva Observación" : "Editar Observación" %>
  </h2>

  <%= render "form" %>
</div>
